# Local Exasol Personal defaults. `exasol info` prints the live values.
AIRLOCK_DSN=127.0.0.1:8563
AIRLOCK_USER=sys
AIRLOCK_PASSWORD=exasol
AIRLOCK_SCHEMA=AIRLOCK
# Agent identity presented to the policy engine.
AIRLOCK_PRINCIPAL=demo-agent
